name: IPTV Status Index

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.8.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm status:index
        env:
          STATUS_INDEX_M3U_URL: ${{ secrets.STATUS_INDEX_M3U_URL }}
          STATUS_INDEX_M3U_TEXT: ${{ secrets.STATUS_INDEX_M3U_TEXT }}
          STATUS_INDEX_LIMIT: "250"
      - run: node --import tsx scripts/generate-worldcup-pages.ts
      # Only paths this job actually writes, and only paths git can hold.
      # `dist/` is in .gitignore and is not in a fresh checkout, so naming
      # `dist/release` here made `git add` exit 128 on every single run from
      # 2026-06-26 onward: the index froze on that date, the deploy steps
      # below never ran again, and the failure was invisible because the site
      # kept serving the last good snapshot.
      - name: Commit status index
        run: |
          if [ -z "$(git status --porcelain -- README.md data apps/worldcup-tv-guide/public)" ]; then
            echo "No status index changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md data apps/worldcup-tv-guide/public
          git commit -m "Update IPTV status index and World Cup TV guide pages"
          git push
      - run: pnpm --filter iptv-doctor-core build
      - run: pnpm --filter iptv-sports-data build
      - run: pnpm --filter @bjia666/match2epg build
      - run: pnpm --filter worldcup-tv-guide build
      - uses: actions/upload-pages-artifact@v4
        with:
          path: apps/worldcup-tv-guide/dist
      - id: deployment
        uses: actions/deploy-pages@v4
